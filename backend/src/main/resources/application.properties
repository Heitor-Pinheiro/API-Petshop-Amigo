spring.application.name=petshop

# Database configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/petshop?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=heitor200827h
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Explicitly set dialect for Hibernate 7.x (Spring Boot 4.x compatibility)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
